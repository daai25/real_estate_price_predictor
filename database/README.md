# PostgreSQL Setup for Property Listings

This project uses a PostgreSQL database to store property listings and associated image URLs.

## Prerequisites

- Python 3.8+
- PostgreSQL installed and running
- `psycopg2` installed:
  ```bash
  pip install psycopg2-binary
